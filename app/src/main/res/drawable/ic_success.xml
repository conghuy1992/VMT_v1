<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <stroke
        android:width="1dp"
        android:color="@color/colorWhite" />
    <corners
        android:bottomLeftRadius="@dimen/text_date"
        android:bottomRightRadius="@dimen/text_date"
        android:topLeftRadius="@dimen/text_date"
        android:topRightRadius="@dimen/text_date" />
    <solid android:color="@color/colorPrimary" />
    <size
        android:width="@dimen/text_date"
        android:height="@dimen/text_date" />

</shape>